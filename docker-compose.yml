services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      POSTGRES_DSN: "postgres://postgres:postgres@db:5432/gowes?sslmode=disable"
    depends_on:
      - db
  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_USER: "postgres"
  #     POSTGRES_PASSWORD: "postgres"
  #     POSTGRES_DB: "gowes"
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
volumes:
  db_data:
